AOP позволяет выделить сквозные задачи:

- **Аннотации**:
    - `@Aspect`: Определяет аспект.
    - `@Before`, `@After`, `@Around`: Определяют поведение перед, после или вокруг вызова метода.
- Примеры задач: логирование, безопасность, обработка ошибок.